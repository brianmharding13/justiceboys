[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# single page, but keep deep links from 404-ing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
